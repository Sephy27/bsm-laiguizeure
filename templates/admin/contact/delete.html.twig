{% extends 'admin/admin.html.twig' %}

{% block title 'contact : ' ~ contact.subject %}

{% block body %}

    <h1>{{ contact.subject }}</h1>

    {{ form(addForm) }}
    <button class="btn btn-lg bg-success text-light mb-5" type="submit">supprimer</button>

{% endblock %}