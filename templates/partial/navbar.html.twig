<nav class="navbar navbar-expand-lg bsm-navbar">
    <div class="container-fluid">

        <a class="navbar-brand d-flex align-items-center" href="{{ path('app_home') }}">
            <img src="/images/marcologo04.png" alt="Logo BSM" class="bsm-logo">
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#bsmNavbar"
                aria-controls="bsmNavbar" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="bsmNavbar">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-lg-center">

                <li class="nav-item">
                    <a class="nav-link bsm-link {% if app.request.get('_route') starts with 'app_home' %}active{% endif %}"
                       href="{{ path('app_home') }}">Accueil</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link bsm-link {% if app.request.get('_route') starts with 'app_contact' %}active{% endif %}"
                       href="{{ path('app_contact') }}">Nous contacter</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link bsm-link {% if app.request.get('_route') starts with 'app_galerie' %}active{% endif %}"
                       href="{{ path('app_galerie') }}">Galerie</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link bsm-link {% if app.request.get('_route') starts with 'admin' %}active{% endif %}"
                       href="{{ path('admin') }}">Admin</a>
                </li>

                <li class="nav-item ms-lg-3 mt-3 mt-lg-0">
                    <form class="bsm-search d-flex" role="search">
                        <input class="form-control bsm-search-input" type="search" placeholder="Rechercher...">
                        <button class="btn bsm-search-btn" type="submit">
                            <i class="bi bi-search"></i>
                        </button>
                    </form>
                </li>

            </ul>
        </div>

    </div>
</nav>
